var velocidade = 82;

console.log(`A velocidade do seu carro é ${velocidade}KM/h}`);

if (velocidade > 60) { // Condição Simples
    console.log('Você está acima da velocidade permitida (60KM/h) => VOCÊ FOI MULTADO!')
}